data:
  data_type: plaintext
  dataloader_type: native
  datasets_type: mapping
  drop_last: true
  max_seq_len: 2048
  num_workers: 2
  prefetch_factor: 2
  text_keys: text
  train_path: /home/johndpope/ds_offload/data_overfit_500.jsonl
  train_size: 500
model:
  attn_implementation: sdpa
  model_path: Qwen/Qwen3-1.7B
train:
  align_layers: 7,14,21,28
  anchor_cache_dir: /home/johndpope/ds_offload/anchors/qwen3-1.7b-overfit
  auto_resume: false
  bsz_warmup_ratio: 0.0
  ckpt_manager: dcp
  data_parallel_mode: ddp
  dyn_bsz_buffer_size: 50
  dyn_bsz_margin: 0
  empty_cache_steps: 50
  enable_activation_offload: true
  enable_fsdp_offload: false
  enable_full_determinism: false
  enable_full_shard: false
  enable_gradient_checkpointing: true
  enable_masking: true
  enable_mixed_precision: true
  freeze_layers: ''
  global_batch_size: 1
  init_device: cuda
  load_checkpoint_path: ''
  lr: 0.0001
  lr_decay_ratio: 1.0
  lr_decay_style: cosine
  lr_min: 1.0e-06
  lr_warmup_ratio: 0.01
  max_grad_norm: 1.0
  micro_batch_size: 1
  num_train_epochs: 500
  optimizer: anyprecision_adamw
  output_dir: /home/johndpope/ds_offload/checkpoints/overfit-no-align
  repr_align_wt: 0.0
  rmpad: false
  rmpad_with_pos_ids: true
  save_epochs: 0
  save_hf_weights: false
  save_steps: 1000000
  save_time_interval_minutes: 0
  ulysses_parallel_size: 1
  use_wandb: true
  wandb_name: 1.7b-no-align-overfit
  wandb_project: open-dllm-overfit
  weight_decay: 0.01
